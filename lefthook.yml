pre-commit:
  commands:
    gitleaks:
      run: betterleaks git --pre-commit --staged
    swift-format:
      glob: "*.swift"
      run: swift format lint --strict {staged_files}

pre-push:
  commands:
    gitleaks:
      run: betterleaks git
